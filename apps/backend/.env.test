# Test Environment Configuration
NODE_ENV=test
PORT=3001

# MongoDB Atlas Configuration for Testing
MONGODB_URI=mongodb+srv://Wisal_user:Shoeb8999941541@cluster0.u1qypmd.mongodb.net/wisal_test?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (test values)
JWT_SECRET=test-secret-key-for-testing-only
JWT_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# PostgreSQL Configuration (not needed for current tests)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=wisal_test
POSTGRES_USER=wisal
POSTGRES_PASSWORD=wisal_password

# Redis Configuration (not needed for current tests)
REDIS_HOST=localhost
REDIS_PORT=6379
